name: namer_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter_survey: '^0.1.4'
  flutter:
    sdk: flutter

  english_words: ^4.0.0
  provider: ^6.0.0
  flutter_login: ^4.1.1
  flutter_native_splash: ^2.2.18
  flutter_secure_storage: ^8.0.0
  http: ^0.13.5
  nb_utils: ^5.0.5
  mobx: ^2.1.4
  json_annotation: ^4.8.0
  flutter_form_builder: ^9.0.0
  form_builder_validators: ^9.0.0
  test: ^1.22.0
  expressions: ^0.2.5
  flutter_hooks: ^0.18.6
  flutter_spinkit: ^5.2.0

flutter_native_splash:
  image: assets/sam_is_now_forever_in_my_debt.png
  color: "#0b5ba5"

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  build_runner: ^2.3.3
  json_serializable: ^6.6.1

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/images/